{
	"name": "ohkami-worker-with-openapi",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "export OHKAMI_WORKER_DEV='' && wrangler deploy --assets pages",
		"dev": "export OHKAMI_WORKER_DEV=1 && wrangler dev",
		"openapi": "wasm-pack build --no-typescript --no-pack --target nodejs --out-dir worker_openapi"
	},
	"devDependencies": {
		"wrangler": "^3.50"
	}
}
