[package]
name    = "ohkami_worker"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
ohkami = { version = "0.17", features = ["rt_worker"] }
worker = { version = "0.1.0" }

[profile.release]
opt-level = "s"